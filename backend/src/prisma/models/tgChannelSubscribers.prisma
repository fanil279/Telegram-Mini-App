model TgChannelSubscriber {

    id BigInt @id @default(autoincrement())

    user User @relation(fields: [userId], references: [id], onDelete: Cascade)
    userId BigInt

    channelId BigInt

    joinedAt DateTime @default(now())
    leftAt DateTime?

    @@index([userId, channelId])

}
