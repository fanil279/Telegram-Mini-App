model Hobby {

    id BigInt @id @default(autoincrement())

    name String @unique @db.VarChar(32)

    createdAt DateTime @default(now()) @map("created_at")

    @@map("hobbies")

    @@schema("base")


    prefferedHobbies PrefferedHobby[]

}